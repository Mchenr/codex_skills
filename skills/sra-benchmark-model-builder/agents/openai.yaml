interface:
  display_name: "SRA Benchmark Model Builder"
  short_description: "Build and validate SRA benchmark SavedModels"
  default_prompt: "Use $sra-benchmark-model-builder to build a predictor-server benchmark model from a TensorFlow reference graph, then validate it with SavedModel and brpc serving checks."
